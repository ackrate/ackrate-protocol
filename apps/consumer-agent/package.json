{
  "name": "@ackrate/consumer-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Reference ResearchAgent: signs/registers a mandate and pays via @ackrate/core agent.fetch.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/research-agent.ts",
    "test": "node --import tsx --test src/research-agent.test.ts src/receipt-store.test.ts src/outcome-store.test.ts"
  },
  "dependencies": {
    "@ackrate/core": "*"
  }
}
